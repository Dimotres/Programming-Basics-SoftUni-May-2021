function demo(input) {
    let text = input[0]
    let sum = 0;
    for (let i = 3; i < text.lenght; i++) {
        switch (text[i]) {
            case "a": sum += 1;
        
        }
    }


}
demo(["hi"])